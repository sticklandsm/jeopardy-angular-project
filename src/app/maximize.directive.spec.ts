import { MaximizeDirective } from './maximize.directive';

describe('MaximizeDirective', () => {
  it('should create an instance', () => {
    const directive = new MaximizeDirective();
    expect(directive).toBeTruthy();
  });
});
